public class Calculator {
    public Calculator(){}

    public double calcSolve(String expression) {
        if (expression.indexOf("*") != -1) {
            int index = expression.indexOf("*");
        } else if (expression.indexOf("/") != -1)
    }

}
